export default () => {
  console.log(`
wtc-ts 1.0.2

USAGE:
    wtc-ts [OPTIONS] [SUBCOMMAND]

OPTIONS:
    -h, --help        Print help information and command usages
    -d, --domain      Shows tld information for domain [example: ns.uz]
    -s, --server      Starts server with given protocols [--tcp | --http]
`);
};
